@model ProjetoCardapio.Models.Presencas

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Presencas</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SalasId" class="control-label"></label>
                <select asp-for="SalasId" class ="form-control" asp-items="ViewBag.SalasId"></select>
            </div>
            <div class="form-group">
                <label asp-for="QuantidadeAlunos" class="control-label"></label>
                <input asp-for="QuantidadeAlunos" class="form-control" />
                <span asp-validation-for="QuantidadeAlunos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
